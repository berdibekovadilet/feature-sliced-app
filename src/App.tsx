import styles from "./app.module.scss";

const App = () => {
  return (
    <div className="app">
      App
      <button className={styles.btn}>Adilet</button>
    </div>
  );
};

export default App;
